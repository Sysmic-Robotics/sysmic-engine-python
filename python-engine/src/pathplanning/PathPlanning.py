from RRTAlgorithm import RRTAlgorithm
from RRTMap import RRTMap

# Unica funcion que debe que llamarse de este modulo
def GetPath(point_from, point_to):
    rtt = RRTAlgorithm()
    # Faltan obtener los obstaculos (robots y pelota) en una lista de coordenadas
    #map = RRTMap(MAP_H, MAP_W,  )
    #path = rtt.GetPath(map, point_from, point_to) 
    #return path